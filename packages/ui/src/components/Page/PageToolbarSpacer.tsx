import type { FC } from "react";

import styles from "./PageToolbarSpacer.module.css";

const PageToolbarSpacer: FC = () => {
  return <div className={styles.pageToolbarSpacer} />;
};

export default PageToolbarSpacer;
